<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 다른 mapper와 중복되지 않도록 네임스페이스 기재 -->
<mapper namespace="board">
	<select id="listAll" resultType="com.TeamProject.Kdemy.model.board.dto.BoardDTO">
		<include refid="paging_header" />
		select bno, title, writer, regdate, viewcnt, show,
		(select count(*) from board_reply where bno=b.bno) cnt
		from kdemyboard b
		order by bno desc
			
		<include refid="paging_footer" />
	</select>
	
		<select id="searchlistAll" resultType="com.TeamProject.Kdemy.model.board.dto.BoardDTO">
		<include refid="paging_header" />
		select bno, title, writer,  regdate, viewcnt, show,
		(select count(*) from board_reply where bno=b.bno) cnt
		from kdemyboard b
			<choose>
				<when test="search_option== 'all'">
			where show='Y'
				    and (title like #{keyword}
				        or content like #{keyword} )			
				</when>
			</choose>
		order by bno desc
			
		<include refid="paging_footer" />
	</select>
	
		<sql id="paging_header">
		select *
		from (
		    select rownum as rn, A.*
		    from (	
	</sql>
	
	<sql id="paging_footer">
		    ) A
		) where rn between #{start} and #{end}	
	</sql>
	
	<select id="countArticle" resultType="int">
		select count(*) from kdemyboard b
	</select>
	
	<select id="searchcountArticle" resultType="int">
		select count(*) from kdemyboard b
			<choose>
				<when test="search_option== 'all'">
			where show='Y'
				    and (title like #{keyword}
				        or content like #{keyword} )			
				</when>
			</choose>
	</select>
	
</mapper>














